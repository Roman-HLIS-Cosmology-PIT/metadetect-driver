# metadetect-driver

# Installation

## Environment
Make conda environment from the `environment.yml` file:
```bash
conda env create -f environment.yml
```

## Install package
```bash
pip install -e .
```